import 'package:flutter/material.dart';

const KOUtlineInputBorderFocus = OutlineInputBorder(
  borderRadius: BorderRadius.all(
    Radius.circular(20.0),
  ),
  borderSide: BorderSide(color: Color(0xff3192fa), width: 2.0),
);

const KOutlineInputBorderEnalbe = OutlineInputBorder(
  borderRadius: BorderRadius.all(
    Radius.circular(20.0),
  ),
  borderSide: BorderSide(color: Color(0xff3192fa), width: 2.0),
);

const KDivider = Divider(
  height: 60,
  thickness: 2,
  indent: 160,
  endIndent: 170,
  color: Colors.black,
);



